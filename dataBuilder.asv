head = ['*************                 *************']
head = ['*************                 *************']

clear all;
close all;

picsNo = 581;

dataMatrix = zeros(picsNo, 3, 64);
for imno = 0: picsNo
    i = imread(['c:\pics\a(', int2str(imno), ').jpg']);

    head = ['*************         ' int2str(imno) '        *************']

    dataMatrix(imno) = Guide1(i);


end;